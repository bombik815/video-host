[project]
name = "fastapi-url-shortener"
version = "0.1.0"
description = "FatAPI URL Shortener"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=26.1.0",
    "fastapi>=0.116.2",
    "uvicorn>=0.35.0",
    "redis[hiredis]>=7.4.0",
    "typer>=0.17.4",
]
