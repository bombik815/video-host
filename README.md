# FastAPI URL  Shortener

## Develop:

### Setup:

Right click `url-shortener` -> Mark directory as -> `Sources Root`



### Run:

Go to work directory:
```shell
cd url-shortener
```

Run dev server:
```shell
fastapi dev
```
